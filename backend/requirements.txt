flask>=2.0.0
flask-cors>=3.0.0
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0
gunicorn

# Note: FAISS is optional and not available via pip on Windows
# The app will automatically use sklearn's cosine_similarity as a fallback
# To install FAISS on Windows, use conda: conda install -c conda-forge faiss-cpu

